interface:
  display_name: "test-audit"
  short_description: "Apply the testing policy to new, changed, or existing tests"
  default_prompt: "Use $test-audit to decide whether each test should exist, place it in the unit or e2e tier, keep it deterministic, assess runtime against advisory suite targets, and audit the suite for worthwhile improvements."
