## Why

<!-- The problem in 1–3 sentences, then the effect in numbers. Link the ticket.
     The body must still make sense if the link dies. -->

## What changed

<!-- If the branch touches many files, one line here on where to read first. -->

### <the effect, as a short sentence>

<!-- Two or three bullets, one plain sentence each: what changed, why, and
     what stays the same if the reader would wonder. Then Before and After.
     Ten lines per block or fewer. Real names from the codebase. Same
     structure in both blocks so the difference jumps out. Repeat this ###
     block once per point. -->

- <what a reader sees differently now>
- <why that is better, or what broke before>

Before:

```text
<how it worked>
```

After:

```text
<how it works now>
```

## How to verify

1. <!-- numbered reviewer steps that reach the changed code; "covered by <test>" for awkward cases -->

<!-- End with the exact gate commands that passed on the final head. -->

## Risks & notes

- <!-- broken contracts + deploy order, removed behavior, changed logs or metrics, override-label justification. Delete if none. -->
