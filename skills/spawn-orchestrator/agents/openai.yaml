interface:
  display_name: "Spawn Orchestrator"
  short_description: "Waves of parallel Codex sessions over a backlog"
  default_prompt: "Use $spawn-orchestrator to work the unblocked issues under <epic> in parallel waves: one Codex session per issue in its own worktree off <base>, draft PR back, report, archive, advance."
policy:
  allow_implicit_invocation: false
