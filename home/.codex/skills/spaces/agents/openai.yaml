interface:
  display_name: "Spaces"
  short_description: "Manage Codex worktree-style flows with the local spaces workflow."
  default_prompt: "Use the local spaces workflow for this worktree-style Codex task: all worktree-style management in this repo should go through `spaces` and the `codex --spaces` shell shortcut."
