interface:
  display_name: "Agent Code Review Loop"
  short_description: "Parallel review and verified fixes"
  default_prompt: "Use $agent-code-review-loop to run a parallel code review and verified fix loop on the current branch."
policy:
  allow_implicit_invocation: false
