interface:
  display_name: "gh-ci"
  short_description: "Inspect and classify GitHub Actions logs"
  default_prompt: "Use $gh-ci to produce compact local evidence from these GitHub Actions checks or logs."
policy:
  allow_implicit_invocation: false
